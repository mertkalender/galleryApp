import {
  SafeAreaView,
  ScrollView,
  View,
} from 'react-native';



export function ImageDetailsPage(): JSX.Element {

    return (
      <SafeAreaView >
        <ScrollView contentInsetAdjustmentBehavior="automatic">
          <View>
          </View>
        </ScrollView>
      </SafeAreaView>
    );
  }